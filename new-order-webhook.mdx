---
title: "Webhook for new orders"
api: 'POST /webhooks/new-order/{userId}'
description: "Learn how to send new order data through webhooks"
authMethod: "none"
---

## Overview

The webhook for new orders enables TextYess to receive and manage data from newly created orders. By receiving this data, TextYess can unlock enhanced analytics and provide powerful automation features.

### Path Parameters

<ParamField path="userId" type="string" required initialValue='1234456'>
  The id of the user. You can get your user id from https://ai.textyess.com/developers
</ParamField>

### Body Parameters

<ParamField body="id" type="string" required initialValue="ord_12345678" placeholder="ord_12345678">
  The id of the order
</ParamField>

<ParamField body="shipping_address" type="object" initialValue={{
  first_name: "Alice",
  last_name: "Johnson",
  address1: "123 Main St",
  address2: "Apt 4B",
  phone: "+15552468135",
  city: "New York",
  zip: "10001",
  company: "Tech Innovations Inc",
  country_code: "US",
  province: "NY",
  country: "United States"
}} placeholder="{ first_name: 'Alice', last_name: 'Johnson', ... }">
  Shipping address information
  <Expandable title="Show object properties">
    <ParamField body="first_name" type="string" required initialValue="Alice" placeholder="Alice">
      First name for shipping
    </ParamField>
    <ParamField body="last_name" type="string" required initialValue="Johnson" placeholder="Johnson">
      Last name for shipping
    </ParamField>
    <ParamField body="address1" type="string" required initialValue="123 Main St" placeholder="123 Main St">
      Primary address line
    </ParamField>
    <ParamField body="address2" type="string" initialValue="Apt 4B" placeholder="Apt 4B">
      Secondary address line
    </ParamField>
    <ParamField body="phone" type="string" required initialValue="+15552468135" placeholder="+15552468135">
      Phone number for shipping
    </ParamField>
    <ParamField body="city" type="string" required initialValue="New York" placeholder="New York">
      City for shipping
    </ParamField>
    <ParamField body="zip" type="string" required initialValue="10001" placeholder="10001">
      ZIP or postal code
    </ParamField>
    <ParamField body="company" type="string" initialValue="Tech Innovations Inc" placeholder="Tech Innovations Inc">
      Company name, if applicable
    </ParamField>
    <ParamField body="country_code" type="string" required initialValue="US" placeholder="US">
      Country code
    </ParamField>
    <ParamField body="province" type="string" initialValue="NY" placeholder="NY">
      Province or state
    </ParamField>
    <ParamField body="country" type="string" required initialValue="United States" placeholder="United States">
      Country name
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="billing_address" type="object" initialValue={{
  first_name: "Alice",
  last_name: "Johnson",
  address1: "123 Main St",
  address2: "Apt 4B",
  phone: "+15552468135",
  city: "New York",
  zip: "10001",
  company: "Tech Innovations Inc",
  country_code: "US",
  province: "NY",
  country: "United States"
}} placeholder="{ first_name: 'Alice', last_name: 'Johnson', ... }">
  Billing address information
  <Expandable title="Show object properties">
    <ParamField body="first_name" type="string" required initialValue="Alice" placeholder="Alice">
      First name for billing
    </ParamField>
    <ParamField body="last_name" type="string" required initialValue="Johnson" placeholder="Johnson">
      Last name for billing
    </ParamField>
    <ParamField body="address1" type="string" required initialValue="123 Main St" placeholder="123 Main St">
      Primary address line
    </ParamField>
    <ParamField body="address2" type="string" initialValue="Apt 4B" placeholder="Apt 4B">
      Secondary address line
    </ParamField>
    <ParamField body="phone" type="string" required initialValue="+15552468135" placeholder="+15552468135">
      Phone number for billing
    </ParamField>
    <ParamField body="city" type="string" required initialValue="New York" placeholder="New York">
      City for billing
    </ParamField>
    <ParamField body="zip" type="string" required initialValue="10001" placeholder="10001">
      ZIP or postal code
    </ParamField>
    <ParamField body="company" type="string" initialValue="Tech Innovations Inc" placeholder="Tech Innovations Inc">
      Company name, if applicable
    </ParamField>
    <ParamField body="country_code" type="string" required initialValue="US" placeholder="US">
      Country code
    </ParamField>
    <ParamField body="province" type="string" initialValue="NY" placeholder="NY">
      Province or state
    </ParamField>
    <ParamField body="country" type="string" required initialValue="United States" placeholder="United States">
      Country name
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="created_at" type="string" required initialValue="2023-04-15T14:30:00Z" placeholder="2023-04-15T14:30:00Z">
  The date and time when the order was created
</ParamField>

<ParamField body="email" type="string" initialValue="alice.johnson@example.com" placeholder="alice.johnson@example.com">
  Customer's email address
</ParamField>

<ParamField body="customer" type="object" initialValue={{
  id: "cust_87654321",
  first_name: "Alice",
  last_name: "Johnson",
  phone: "+15552468135"
}} placeholder="{ id: 'cust_87654321', first_name: 'Alice', ... }">
  Customer information
  <Expandable title="Show object properties">
    <ParamField body="id" type="string" required initialValue="cust_87654321" placeholder="cust_87654321">
      Customer's unique identifier
    </ParamField>
    <ParamField body="first_name" type="string" initialValue="Alice" placeholder="Alice">
      Customer's first name
    </ParamField>
    <ParamField body="last_name" type="string" initialValue="Johnson" placeholder="Johnson">
      Customer's last name
    </ParamField>
    <ParamField body="phone" type="string" initialValue="+15552468135" placeholder="+15552468135">
      Customer's phone number
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="order_number" type="number" initialValue={1001} placeholder="1001">
  The order number
</ParamField>

<ParamField body="total_price" type="string" required initialValue="129.99" placeholder="129.99">
  The total price of the order
</ParamField>

<ParamField body="payment_gateway_names" type="array" required initialValue={["stripe", "paypal"]} placeholder="['stripe', 'paypal']">
  An array of payment gateway names used for the order
</ParamField>

<ParamField body="line_items" type="array" required initialValue={[
  {
    price: "49.99",
    quantity: 2,
    title: "Widget A",
    variant_title: "Large"
  },
  {
    price: "29.99",
    quantity: 1,
    title: "Gadget B",
    variant_title: null
  }
]} placeholder="[{ price: '49.99', quantity: 2, title='Widget A'},]">
  An array of items in the order
  <Expandable title="Show array item properties">
    <ParamField body="price" type="string" required initialValue="49.99" placeholder="49.99">
      Price of the item
    </ParamField>
    <ParamField body="quantity" type="number" required initialValue={2} placeholder="2">
      Quantity of the item
    </ParamField>
    <ParamField body="title" type="string" required initialValue="Widget A" placeholder="Widget A">
      Title of the item
    </ParamField>
    <ParamField body="variant_title" type="string" initialValue="Large" placeholder="Large">
      Title of the item variant, if applicable
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="financial_status" type="string" required initialValue="paid" placeholder="paid">
  The financial status of the order
  <Expandable title="Possible values">
    'authorized' | 'pending' | 'paid' | 'partially_paid' | 'refunded' | 'voided' | 'partially_refunded' | 'unpaid'
  </Expandable>
</ParamField>

<ParamField body="phone" type="string" required initialValue="+15552468135" placeholder="+15552468135">
  The phone number associated with the order
</ParamField>

<ParamField body="fulfillments" type="array" initialValue={[
  {
    id: "ful_12345678",
    shipment_status: "shipped",
    tracking_company: "FedEx",
    tracking_url: "https://www.fedex.com/tracking/123456789",
    tracking_urls: ["https://www.fedex.com/tracking/123456789"]
  }
]} >
  An array of fulfillment information
  <Expandable title="Show array item properties">
    <ParamField body="id" type="string" required initialValue="ful_12345678" placeholder="ful_12345678">
      Fulfillment ID
    </ParamField>
    <ParamField body="shipment_status" type="string" required initialValue="shipped" placeholder="shipped">
      Status of the shipment
    </ParamField>
    <ParamField body="tracking_company" type="string" required initialValue="FedEx" placeholder="FedEx">
      Name of the tracking company
    </ParamField>
    <ParamField body="tracking_url" type="string" required initialValue="https://www.fedex.com/tracking/123456789" placeholder="https://www.fedex.com/tracking/123456789">
      URL for tracking the shipment
    </ParamField>
    <ParamField body="tracking_urls" type="string[]" required initialValue={["https://www.fedex.com/tracking/123456789"]} placeholder="['https://www.fedex.com/tracking/123456789']">
      Array of tracking URLs
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="order_status_url" type="string" initialValue="https://example.com/orders/status/ord_12345678" placeholder="https://example.com/orders/status/ord_12345678">
  URL to check the order status
</ParamField>
### Response

<ResponseField name="success" type="boolean">
  If the webhook was sucessfully process
</ResponseField>

<ResponseExample>

```json 200
{ "success": "true" }
```

```json 400
{ "status": "false" }
```

</ResponseExample>

